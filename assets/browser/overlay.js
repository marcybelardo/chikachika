// The renderer owns the initial DOM. This namespace is intentionally limited
// to replacing a complete representation supplied by the native application.
window.ChikachikaOverlay = Object.freeze({
  canvas: document.getElementById("chikachika-canvas"),
});
